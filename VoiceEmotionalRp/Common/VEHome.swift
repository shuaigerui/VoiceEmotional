
import UIKit

import Foundation

enum VE_CurrentUser {

    
    static var user: VEScreen?

    static func signIn(_ model: VEScreen) {
       var activeC: Int = 2
    var transactionsl: String! = String(cString: [103,108,111,98,97,108,115,0], encoding: .utf8)!
    _ = transactionsl
      activeC -= 1 + activeC
      transactionsl = "\(3)"

        user = model
    }

    static func signOut() {
       var fillU: Int = 0
      fillU %= Swift.max(4, fillU)

        user = nil
    }

    static var isLoggedIn: Bool {
       var encK: Double = 2.0
   while ((encK - 5.95) >= 5.58 || (encK - encK) >= 5.95) {
      encK -= (Double(3 | Int(encK > 118110554.0 || encK < -118110554.0 ? 68.0 : encK)))
      break
   }
     return user != nil }
}
