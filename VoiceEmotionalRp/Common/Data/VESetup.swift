
import UIKit

import Foundation

enum UserData {

    
    static let testAccount: VEScreen = {
       var scrollE: String! = String(cString: [115,99,97,108,97,114,115,0], encoding: .utf8)!
    _ = scrollE
    var rawX: Int = 0
   withUnsafeMutablePointer(to: &rawX) { pointer in
    
   }
   for _ in 0 ..< 1 {
      rawX -= 1 >> (Swift.min(4, scrollE.count))
   }

        let u = VEScreen(
            id: 9000,
            name: "Test",
            email: "test@gmail.com",
            password: "123456",
            avatarAssetName: "avatar_15",
            voiceUrl: "moreVideo",
            isFollowed: false,
            isBlock: false,
            coins: 50
        )
   repeat {
      scrollE.append("\(scrollE.count)")
      if 1439823 == scrollE.count {
         break
      }
   } while (1439823 == scrollE.count) && (scrollE == scrollE)
        return u
    }()

    
    static let localUsers: [VEScreen] = makeLocalUsers()

    
    static var registeredUser: VEScreen?

    
    static var allUsersIncludingRegistered: [VEScreen] {
       var formatterU: String! = String(cString: [117,112,100,97,116,101,100,0], encoding: .utf8)!
    var playing6: Double = 5.0
    _ = playing6
   repeat {
      formatterU = "\((Int(playing6 > 338197330.0 || playing6 < -338197330.0 ? 74.0 : playing6) << (Swift.min(formatterU.count, 3))))"
      if formatterU == (String(cString:[105,112,118,122,100,100,121,110,112,0], encoding: .utf8)!) {
         break
      }
   } while (formatterU == (String(cString:[105,112,118,122,100,100,121,110,112,0], encoding: .utf8)!)) && (5.63 < (1.17 * playing6))

        var presented = Array(localUsers)
        if let r = registeredUser,
           !presented.contains(where: { $0.id == r.id }),
           VE_CurrentUser.user?.id == r.id {
            presented.append(r)
        }
        return presented
   for _ in 0 ..< 3 {
      playing6 -= (Double(Int(playing6 > 75868320.0 || playing6 < -75868320.0 ? 58.0 : playing6)))
   }
    }

    
    static var allDistinctUserModels: [VEScreen] {
       var recordingM: String! = String(cString: [114,116,99,100,0], encoding: .utf8)!
   if !recordingM.hasSuffix(recordingM) {
       var clampede: Double = 1.0
      withUnsafeMutablePointer(to: &clampede) { pointer in
    
      }
       var subk: [Any]! = [String(cString: [108,97,122,105,108,121,0], encoding: .utf8)!]
      withUnsafeMutablePointer(to: &subk) { pointer in
             _ = pointer.pointee
      }
       var cfgy: Int = 2
       var middleE: Float = 0.0
         subk.append((Int(middleE > 355380421.0 || middleE < -355380421.0 ? 20.0 : middleE)))
      repeat {
         clampede -= Double(cfgy)
         if clampede == 2256328.0 {
            break
         }
      } while ((subk.count * 3) <= 5) && (clampede == 2256328.0)
          var authv: Float = 1.0
          var baseA: String! = String(cString: [99,111,110,116,114,111,108,108,101,100,0], encoding: .utf8)!
          var users2: [String: Any]! = [String(cString: [102,97,107,101,0], encoding: .utf8)!:String(cString: [114,116,102,0], encoding: .utf8)!, String(cString: [98,97,99,107,0], encoding: .utf8)!:String(cString: [115,104,97,114,101,115,0], encoding: .utf8)!]
         middleE += Float(users2.values.count ^ baseA.count)
         authv -= (Float(3 | Int(authv > 198591942.0 || authv < -198591942.0 ? 90.0 : authv)))
       var step5: [Any]! = [String(cString: [112,101,105,114,115,0], encoding: .utf8)!, String(cString: [113,117,97,110,116,105,122,97,116,105,111,110,0], encoding: .utf8)!, String(cString: [98,105,116,0], encoding: .utf8)!]
      withUnsafeMutablePointer(to: &step5) { pointer in
    
      }
      if 2 <= (4 & step5.count) && (step5.count & subk.count) <= 4 {
         step5.append(subk.count >> (Swift.min(labs(1), 1)))
      }
      while (3 < (step5.count + cfgy) && (step5.count + 3) < 1) {
         cfgy /= Swift.max((2 | Int(clampede > 341632260.0 || clampede < -341632260.0 ? 90.0 : clampede)), 5)
         break
      }
      while (3 >= (subk.count * 3) || 2.8 >= (clampede + Double(subk.count))) {
         clampede -= (Double(cfgy / (Swift.max(2, Int(clampede > 124382998.0 || clampede < -124382998.0 ? 81.0 : clampede)))))
         break
      }
       var namesS: [Any]! = [20, 31, 84]
       var k_managerR: [Any]! = [75, 6]
       _ = k_managerR
         middleE += (Float(Int(clampede > 224142349.0 || clampede < -224142349.0 ? 24.0 : clampede) / 3))
         step5.append(1)
         k_managerR = [(Int(middleE > 323909183.0 || middleE < -323909183.0 ? 59.0 : middleE) << (Swift.min(step5.count, 3)))]
         step5 = [(Int(clampede > 154824297.0 || clampede < -154824297.0 ? 5.0 : clampede) ^ namesS.count)]
      recordingM = "\(2)"
   }

        var presented = Array(localUsers)
        if let r = registeredUser, !presented.contains(where: { $0.id == r.id }) {
            presented.append(r)
        }
        return presented
    }

    private static func makeLocalUsers() -> [VEScreen] {
       var credential4: Bool = false
   withUnsafeMutablePointer(to: &credential4) { pointer in
          _ = pointer.pointee
   }
      credential4 = (!credential4 ? !credential4 : !credential4)

        let date = VEScreen(
            id: 1001,
            name: "Wren",
            email: "wren@gmail.com",
            password: "",
            avatarAssetName: "avatar_05",
            voiceUrl: "followStep",
            isFollowed: false,
            isBlock: false
        )
        let display = VEScreen(
            id: 1002,
            name: "Luna",
            email: "luna@gmail.com",
            password: "",
            avatarAssetName: "avatar_03",
            voiceUrl: "showAudioLongin",
            isFollowed: true,
            isBlock: false
        )
        let remaining = VEScreen(
            id: 1003,
            name: "Swft",
            email: "Swft@gmail.com",
            password: "",
            avatarAssetName: "avatar_08",
            voiceUrl: "settingError",
            isFollowed: false,
            isBlock: false
        )
        let release_3 = VEScreen(
            id: 1004,
            name: "Marceline",
            email: "marceline@example.com",
            password: "",
            avatarAssetName: "avatar_02",
            voiceUrl: "mainDelegate_k",
            isFollowed: true,
            isBlock: false
        )

        date.posts = [
            VERegisterSetup(
                img: [.assetCatalog(name: "post_05")],
                id: 1001_01,
                des: "Life always has moments that make you feel exhilarated.",
                user: date,
                commons: [
                    VEDelegateBlack(user: display, commit: "Same here, thanks for sharing."),
                    VEDelegateBlack(user: remaining, commit: "Beautiful words."),
                ],
                isReport: false,
                emojiType: 1,
                isLike: false,
            ),
        ]

        display.posts = [
            VERegisterSetup(
                img: [.assetCatalog(name: "post_03")],
                id: 1002_01,
                des: "Some quiet half nights can heal a thousand words",
                user: display,
                commons: [
                    VEDelegateBlack(user: date, commit: "Absolutely."),
                ],
                isReport: false,
                emojiType: 0,
                isLike: true,
            ),
        ]

        remaining.posts = [VERegisterSetup(
            img: [.assetCatalog(name: "post_01")],
            id: 1003_01,
            des: "I argue with my wife every day, and I'm really fed up with it.",
            user: display,
            commons: [
                VEDelegateBlack(user: date, commit: "Absolutely."),
            ],
            isReport: false,
            emojiType: 0,
            isLike: true,
        ),]

        release_3.posts = [
            VERegisterSetup(
                img: [.assetCatalog(name: "post_02")],
                id: 1004_01,
                des: "Tiny sparks of joy still count, even on complicated days.",
                user: release_3,
                commons: [
                    VEDelegateBlack(user: date, commit: "You two should have a proper talk; this is really draining your relationship."),
                ],
                isReport: false,
                emojiType: 2,
                isLike: true,
            ),
        ]

        
        testAccount.posts = [
            VERegisterSetup(
                img: [.assetCatalog(name: "post_04")],
                id: 900_001,
                des: "There are always moments in life that make you want to give up! Life is too difficult!",
                user: testAccount,
                commons: [
                    VEDelegateBlack(user: display, commit: "Same here, thanks for sharing."),
                    VEDelegateBlack(user: remaining, commit: "Beautiful words."),
                ],
                isReport: false,
                emojiType: 0,
                isLike: false,
            ),
        ]

        return [date, display, remaining, release_3, testAccount]
    }

    
    static func resetTestAccountToFactoryDefaults() {
       var default_eF: String! = String(cString: [98,97,103,101,0], encoding: .utf8)!
    var friendsn: Int = 1
      friendsn &= friendsn

       var snapshotv: String! = String(cString: [97,118,115,116,114,105,110,103,0], encoding: .utf8)!
       var stateq: String! = String(cString: [110,101,119,116,101,107,0], encoding: .utf8)!
       var reusey: String! = String(cString: [116,114,97,118,101,114,115,101,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &reusey) { pointer in
    
      }
         snapshotv = "\(snapshotv.count)"
      for _ in 0 ..< 3 {
          var linef: String! = String(cString: [99,101,108,101,98,114,97,116,101,0], encoding: .utf8)!
          var headerH: String! = String(cString: [112,114,101,97,108,108,111,99,0], encoding: .utf8)!
          var inputQ: String! = String(cString: [109,97,120,107,101,121,115,105,122,101,0], encoding: .utf8)!
          var minimumr: String! = String(cString: [112,114,111,109,105,115,101,115,0], encoding: .utf8)!
         snapshotv = "\((minimumr == (String(cString:[82,0], encoding: .utf8)!) ? minimumr.count : reusey.count))"
         linef = "\(inputQ.count & linef.count)"
         headerH = "\(3)"
         inputQ.append("\(3)")
      }
         stateq.append("\(stateq.count >> (Swift.min(labs(1), 1)))")
      while (snapshotv.count <= 3) {
         snapshotv = "\(2)"
         break
      }
         snapshotv = "\(stateq.count - 2)"
      while (stateq.count > reusey.count) {
         stateq = "\(snapshotv.count % 2)"
         break
      }
         stateq = "\(((String(cString:[120,0], encoding: .utf8)!) == reusey ? stateq.count : reusey.count))"
       var pathg: String! = String(cString: [115,115,101,100,105,102,102,0], encoding: .utf8)!
       var ratioY: String! = String(cString: [115,121,109,109,101,116,114,105,99,0], encoding: .utf8)!
      if reusey.count < pathg.count {
          var weekw: String! = String(cString: [114,112,99,0], encoding: .utf8)!
          var duplicateL: Float = 2.0
          _ = duplicateL
          var ring8: [Any]! = [20, 14]
          var reservedf: String! = String(cString: [114,101,115,116,114,105,99,116,0], encoding: .utf8)!
          var s_layerP: String! = String(cString: [114,97,98,98,105,116,0], encoding: .utf8)!
         reusey.append("\(1)")
         weekw.append("\(((String(cString:[113,0], encoding: .utf8)!) == snapshotv ? ring8.count : snapshotv.count))")
         duplicateL += Float(2 & ratioY.count)
         ring8.append(reservedf.count << (Swift.min(labs(2), 5)))
         reservedf.append("\(weekw.count + 2)")
         s_layerP = "\(weekw.count - 2)"
      }
      default_eF = "\(((String(cString:[104,0], encoding: .utf8)!) == reusey ? reusey.count : default_eF.count))"
        let session = testAccount
        session.name = "Test"
        session.email = "test@gmail.com"
        session.password = "123456"
        session.avatarAssetName = "avatar_01"
        session.customAvatarBaseName = nil
        session.voiceUrl = "moreVideo"
        session.isFollowed = false
        session.isBlock = false
        session.coins = 0
        guard let luna = localUsers.first(where: { $0.id == 1002 }),
              let remaining = localUsers.first(where: { $0.id == 1003 })
        else { return }
        session.posts = [
            VERegisterSetup(
                img: [.assetCatalog(name: "post_04")],
                id: 900_001,
                des: "There are always moments in life that make you want to give up! Life is too difficult!",
                user: session,
                commons: [
                    VEDelegateBlack(user: luna, commit: "Same here, thanks for sharing."),
                    VEDelegateBlack(user: remaining, commit: "Beautiful words."),
                ],
                isReport: false,
                emojiType: 0,
                isLike: false,
            ),
        ]
    }

    
    
    
    static var displayUsersIncludingCurrent: [VEScreen] {
       var idsc: Bool = false
       var valueZ: String! = String(cString: [97,116,116,114,105,98,117,116,101,115,0], encoding: .utf8)!
      repeat {
         valueZ.append("\(valueZ.count * valueZ.count)")
         if valueZ.count == 2181688 {
            break
         }
      } while (valueZ.count == 2181688) && (valueZ.count < valueZ.count)
      for _ in 0 ..< 1 {
         valueZ.append("\(valueZ.count % 2)")
      }
      repeat {
         valueZ = "\(valueZ.count << (Swift.min(labs(2), 3)))"
         if valueZ.count == 1554808 {
            break
         }
      } while (valueZ != valueZ) && (valueZ.count == 1554808)
      idsc = valueZ.count <= 48 || idsc

        let logged = localUsers.filter { $0.id != testAccount.id && !$0.isBlock }
        guard let current = VE_CurrentUser.user else { return logged }
        return logged + [current]
    }
}

extension UserData {


    
    static func appendComment(postId: Int64, comment: VEDelegateBlack) {
       var deviceD: Double = 0.0
    var clampedt: Int = 5
   while (2.28 == (Double(Int(deviceD) * clampedt))) {
      clampedt |= (clampedt + Int(deviceD > 283976590.0 || deviceD < -283976590.0 ? 34.0 : deviceD))
      break
   }

        for user in allDistinctUserModels {
            guard let index = user.posts.firstIndex(where: { $0.id == postId }) else { continue }
            user.posts[index].commons.append(comment)
            return
        }
    }


    
    static func post(withId id: Int64) -> VERegisterSetup? {
       var show6: Bool = false
       var termsG: String! = String(cString: [97,116,116,114,97,99,116,105,111,110,0], encoding: .utf8)!
       var q_positionA: String! = String(cString: [105,110,118,101,114,116,101,100,0], encoding: .utf8)!
         termsG = "\(2)"
      for _ in 0 ..< 3 {
         termsG = "\(q_positionA.count)"
      }
         termsG.append("\(termsG.count * q_positionA.count)")
      if 3 > termsG.count {
          var sorted8: Bool = true
         termsG.append("\((q_positionA.count + (sorted8 ? 4 : 4)))")
      }
      repeat {
         termsG.append("\(termsG.count >> (Swift.min(q_positionA.count, 4)))")
         if termsG == (String(cString:[56,114,116,52,99,51,103,107,53,98,0], encoding: .utf8)!) {
            break
         }
      } while (q_positionA.count > 2) && (termsG == (String(cString:[56,114,116,52,99,51,103,107,53,98,0], encoding: .utf8)!))
          var placeholderM: Float = 3.0
          var calendart: String! = String(cString: [119,105,116,104,0], encoding: .utf8)!
          var submitP: String! = String(cString: [97,112,101,114,0], encoding: .utf8)!
         termsG = "\(3 + calendart.count)"
         placeholderM += Float(submitP.count * q_positionA.count)
         submitP = "\(submitP.count)"
      show6 = 3 >= termsG.count || q_positionA == (String(cString:[73,0], encoding: .utf8)!)

        for user in allDistinctUserModels {
            if let post = user.posts.first(where: { $0.id == id }) {
                return post
            }
        }
        return nil
    }


    
    static func user(withId id: Int64) -> VEScreen? {
       var diameterp: Bool = false
    _ = diameterp
    var tap0: Float = 5.0
   while (diameterp && (4.69 * tap0) == 2.38) {
      diameterp = !diameterp
      break
   }

      tap0 /= Swift.max(5, (Float(2 - Int(tap0 > 286619505.0 || tap0 < -286619505.0 ? 16.0 : tap0))))
        if registeredUser?.id == id { return registeredUser }
        return localUsers.first { $0.id == id }
    }


    
    static func setPostLike(postId: Int64, isLike: Bool) {
       var hintq: Int = 4
    var postsa: Double = 2.0
   withUnsafeMutablePointer(to: &postsa) { pointer in
          _ = pointer.pointee
   }
      hintq &= (Int(postsa > 111380654.0 || postsa < -111380654.0 ? 62.0 : postsa) << (Swift.min(4, labs(hintq))))

        for user in allDistinctUserModels {
            guard let index = user.posts.firstIndex(where: { $0.id == postId }) else { continue }
            user.posts[index].isLike = isLike
      postsa -= (Double(Int(postsa > 94581903.0 || postsa < -94581903.0 ? 73.0 : postsa) & Int(postsa > 290381980.0 || postsa < -290381980.0 ? 70.0 : postsa)))
            return
        }
    }


    
    static func setPostReported(postId: Int64, isReport: Bool) {
       var guidea: [Any]! = [21, 8, 41]
   while (guidea.count >= 4) {
       var recognizerd: String! = String(cString: [105,110,116,101,114,112,114,101,116,0], encoding: .utf8)!
       var resourcec: Double = 5.0
      withUnsafeMutablePointer(to: &resourcec) { pointer in
             _ = pointer.pointee
      }
       var recordsS: Bool = true
       var person7: String! = String(cString: [110,97,108,117,0], encoding: .utf8)!
       var houro: String! = String(cString: [103,101,110,101,114,97,103,101,0], encoding: .utf8)!
      for _ in 0 ..< 3 {
         resourcec /= Swift.max(5, (Double(Int(resourcec > 46035908.0 || resourcec < -46035908.0 ? 3.0 : resourcec) / (Swift.max(6, (recordsS ? 1 : 3))))))
      }
         houro = "\(((recordsS ? 3 : 2) % 2))"
         recognizerd.append("\(((recordsS ? 1 : 4) >> (Swift.min(person7.count, 5))))")
         recordsS = houro.count > 80
      for _ in 0 ..< 1 {
          var randomr: Double = 2.0
          var weekdays: Float = 2.0
         withUnsafeMutablePointer(to: &weekdays) { pointer in
    
         }
          var max_gzy: Int = 3
          _ = max_gzy
          var regexj: Int = 5
         recordsS = recognizerd.count < 47
         randomr -= Double(1)
         weekdays /= Swift.max(1, Float(person7.count + 3))
         max_gzy ^= (1 % (Swift.max(2, (recordsS ? 1 : 1))))
         regexj *= (Int(weekdays > 244147018.0 || weekdays < -244147018.0 ? 94.0 : weekdays) ^ 3)
      }
      if !houro.hasPrefix("\(recognizerd.count)") {
         recognizerd = "\(recognizerd.count % (Swift.max(3, 9)))"
      }
      for _ in 0 ..< 3 {
         person7.append("\(1 << (Swift.min(4, person7.count)))")
      }
       var codes4: String! = String(cString: [107,101,121,98,111,97,114,100,0], encoding: .utf8)!
       var max_fw: String! = String(cString: [114,101,111,114,100,101,114,101,100,0], encoding: .utf8)!
      while (codes4 != recognizerd) {
         recognizerd.append("\(((String(cString:[118,0], encoding: .utf8)!) == houro ? houro.count : (recordsS ? 1 : 3)))")
         break
      }
      repeat {
          var users1: Bool = false
         withUnsafeMutablePointer(to: &users1) { pointer in
                _ = pointer.pointee
         }
         recordsS = users1
         if recordsS ? !recordsS : recordsS {
            break
         }
      } while (recordsS ? !recordsS : recordsS) && (person7.hasSuffix("\(recordsS)"))
       var confirmF: String! = String(cString: [114,97,100,105,97,110,115,0], encoding: .utf8)!
      repeat {
         codes4.append("\(houro.count)")
         if codes4 == (String(cString:[119,98,119,55,101,118,106,51,110,101,0], encoding: .utf8)!) {
            break
         }
      } while (codes4 == (String(cString:[119,98,119,55,101,118,106,51,110,101,0], encoding: .utf8)!)) && (!codes4.hasPrefix(confirmF))
      while (recordsS || 2 <= person7.count) {
          var audior: Bool = false
         recordsS = max_fw.count >= 28
         audior = (person7.count * houro.count) < 12
         break
      }
      if 4 < (3 * person7.count) || 4.2 < (resourcec * Double(person7.count)) {
         resourcec -= Double(confirmF.count)
      }
      if 3.47 == resourcec {
         recognizerd.append("\(2)")
      }
      guidea.append(recognizerd.count)
      break
   }

        for user in allDistinctUserModels {
            guard let index = user.posts.firstIndex(where: { $0.id == postId }) else { continue }
            user.posts[index].isReport = isReport
            return
        }
    }
}
